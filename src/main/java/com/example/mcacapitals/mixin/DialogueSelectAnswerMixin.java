package com.example.mcacapitals.mixin;

/**
 * Disabled.
 *
 * Petition handling now uses a custom MCA dialogue action registration
 * instead of a runtime injection into Dialogues.selectAnswer(...).
 */
public class DialogueSelectAnswerMixin {
}